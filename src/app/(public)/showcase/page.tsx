import { ShowcasePage } from "@/components/marketing/showcase-page";

export const metadata = {
  title: "Component showcase — Alpina CRM",
  description:
    "Every shadcn/ui component we use inside Alpina CRM, grouped by role with links to the routes that demo them. A spec sheet for technical stakeholders.",
};

export default function Page() {
  return <ShowcasePage />;
}
